# Leave empty when the frontend and FastAPI are served from the same origin.
VITE_API_BASE_URL=

# Vite-only development proxy target. Never exposed to browser code.
VITE_DEV_API_TARGET=http://127.0.0.1:8200
