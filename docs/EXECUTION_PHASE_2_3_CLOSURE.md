# QuantDesk 第二、第三阶段执行内核收尾基线

> 基线日期：2026-08-30  
> 原始收尾基线：`d13d6a2`
> 当前部署提交：`5476568`
> 状态：第二阶段已完成；第三阶段开发验收已完成，仅剩真实资金 Canary 观察。
> 约束：不改变当前 UI；不把架构重构和交易策略调参混在同一提交；所有生产变更先修改本地、提交 Git，再由服务器拉取部署。

## 1. 本轮目标

第二、第三阶段只解决一件事：让回测、模拟盘、Shadow 和实盘共享同一套确定性策略语义与执行写入内核，运行模式之间只保留行情源、时钟、撮合模型和 Broker 的差异。

```text
MarketEvent
  -> StrategyDecisionService
  -> DecisionEnvelope
  -> Risk / PositionPolicy
  -> ExitPolicy
  -> OrderIntent
  -> ExecutionService
  -> Broker adapter
  -> Execution / Position facts
```

实盘的 Binance 下单、保护单、订单对账、持仓同步和异常恢复是基础设施适配能力，不得再包含一套独立的策略方向、周期或止盈止损判断。

## 2. 当前真实完成度

| 范围 | 当前完成度 | 已完成 | 尚未完成 |
| --- | ---: | --- | --- |
| 第一阶段：统一语义 | 约 90% | `TimeframePolicy`、`ExitPolicy`、`DecisionEnvelope` 已建立；模拟盘固定 4 小时已移除；实盘盈利保护计算已进入领域策略；跨模式固定金样已覆盖八类决策和退出语义 | 实盘 Canary 仍需用同一语义签名做生产观察；盈利保护目前只在实盘产生实际调用 |
| 第二阶段：接管 Shadow / 模拟盘 | 100% | Shadow 和模拟盘订单写入已使用 `ExecutionService`；幂等 claim 已持久化；成交事实、重启恢复、余额对账、跨模式金样、legacy 生产截止以及 applied 历史事实治理均已闭环 | 开发验收已完成；上线后继续保留常规监控 |
| 第三阶段：接管实盘 | 约 90% | 实盘市价单、保护单、订单对账、持仓投影、保护恢复和账户恢复均已进入公共应用服务；四类故障自动化测试已覆盖；新增只读 `audit-live` | 仅剩部署后的独立 Canary 完整观察窗口 |

总体六阶段粗略完成度约 55%。第二阶段可标记为完成；第三阶段开发和自动化故障验收已完成，但在独立 Canary 完整观察窗口通过前仍不能标记生产验收完成。

## 3. 已落地的可验证证据

自原审计基线 `ee2e7af` 起，已完成以下提交：

- `9724a53`：统一策略持仓周期。
- `bcc8a3e`：统一退出价位计算。
- `2c54736`：记录统一退出决策。
- `7f620df`：统一第一阶段策略语义对象。
- `eb138a1`：Shadow 接入统一执行服务和持久幂等日志。
- `d13d6a2`：模拟盘和实盘订单写入接入统一执行服务。

现有关键资产必须保留并继续建设：

- Python 策略沙箱和不可变策略修订；
- `DecisionEnvelope`、`OrderIntent`、Execution Journal；
- worker 心跳、租约、幂等 claim 和恢复租约；
- Binance 对账、保护单与物理账户绑定；
- 风险控制和审计内核（即使菜单不展示也不能从执行链删除）。

## 4. 第二阶段收尾验收门槛

以下条件必须全部满足，第二阶段才能标记完成：

- [x] `shadow_worker` 不导入 `paper_engine` 私有函数。
- [x] Shadow 同一决策只创建一个持久化订单意图。
- [x] Shadow worker 重启后不会为同一决策重复成交。
- [x] 模拟盘订单写入统一 `ExecutionService`。
- [x] 模拟成交事实写入持久执行日志。
- [x] 模拟盘启动时可用 Execution / Fill 事实重建或核对持仓，不依赖“写完兼容表就一定成功”的假设。
- [x] 模拟盘执行日志与 `paper_positions`、余额和交易流水具有可重复运行的待投影恢复任务。
- [x] 对已标记 applied 的历史事实执行余额 dry-run 审计；只重建检查点之后、事实完整且余额闭合的缺失流水，历史权益缺口不伪造。
- [x] 同一策略修订、同一 K 线输入在回测、模拟和 Shadow 中产生相同方向、周期、退出价位和退出原因。
- [x] `legacy_score_v1` 不再是新账户可选主链路；迁移保存原模式、迁移时间和截止 revision，历史事实继续可读。

## 5. 第三阶段收尾验收门槛

以下条件必须全部满足，第三阶段才能标记完成：

- [x] `live_engine` 不再导入 `paper_engine` 的任何私有函数或常量。
- [x] `live_engine` 内不存在绕过 Broker / `ExecutionService` 的 Binance 市价单直连调用。
- [x] `live_engine` 内不存在绕过 `ProtectionService` 的保护单直连调用。
- [x] 所有新开仓和平仓先产生持久、确定性、可恢复的订单意图。
- [x] 实盘保护单通过 Binance Broker 适配器安装。
- [x] 订单对账、持仓同步、保护单修复、异常恢复分别形成明确应用服务，`live_engine` 只负责编排和执行恢复动作。
- [x] 开仓成交后执行“刷新账户事实 -> 结算风险预留 -> 安装并核验保护单”；任一步失败进入持久可恢复的安全状态。
- [x] worker 重启、请求超时、成交回写失败和保护单部分成功四类自动化故障测试通过，且不重复开仓、不静默遗留裸仓。
- [ ] Canary 账户运行至少一个完整观察窗口，确认无重复意图、无长期 UNKNOWN、无未保护持仓后才扩大范围。

## 6. 本轮工作包

### P2/P3-A：消除跨运行器私有依赖

1. 将账户策略求值、策略决策审计和入场快照提升为公共应用服务。
2. `paper_engine` 与 `live_engine` 仅通过公共服务调用。
3. `live_engine` 的退出价位直接使用领域 `ExitPolicy`，不再回调模拟盘函数。
4. 增加架构边界测试，禁止 `live_engine -> paper_engine` 回归。

### P3-B：删除绕过统一内核的旧写路径

1. 删除已停用的 Binance 市价单直连函数。
2. 删除已停用的保护单直连函数及其专用回滚函数。
3. 增加静态边界测试：运行器不得直接调用交易客户端的下单方法。
4. 保留只读查询、撤单与对账能力，直到对应应用服务完成迁移。

### P2-C：模拟盘事实对账

1. 建立从 Execution / Fill 到模拟持仓、余额和交易流水的幂等投影。
2. 增加启动时差异检测与修复任务。
3. 将兼容表明确降级为 Projection，而不是订单事实来源。

### P3-D：拆分实盘恢复能力

按顺序抽取：

1. `LiveOrderReconciliationService`
2. `LivePositionSyncService`
3. `ProtectionRecoveryService`
4. `LiveAccountRecoveryService`

每抽取一个服务，都先做行为等价测试，再删除 `live_engine` 中对应旧实现。不得一次性重写。

## 7. 一致性与故障验收矩阵

| 场景 | 回测 | 模拟盘 | Shadow | 实盘 Canary |
| --- | --- | --- | --- | --- |
| 多/空/观望方向 | 必须一致 | 必须一致 | 必须一致 | 必须一致 |
| 触发周期与信号有效期 | 必须一致 | 必须一致 | 必须一致 | 必须一致 |
| 初始止损、止盈 | 必须一致 | 必须一致 | 必须一致 | 允许交易所步长取整 |
| 最大持有与退出原因 | 必须一致 | 必须一致 | 必须一致 | 必须一致 |
| 手续费与滑点 | 模型值 | 模型值 | 模型值 | 交易所事实 |
| 重启重复订单 | 不适用 | 必须为 0 | 必须为 0 | 必须为 0 |
| UNKNOWN 恢复 | 不适用 | 必须闭环 | 必须闭环 | 必须通过交易所对账闭环 |
| 保护单部分失败 | 不适用 | 模拟故障 | 模拟故障 | 必须进入安全恢复，禁止静默裸仓 |

## 8. 上线与回滚规则

1. 本地修改、测试、提交并推送 Git。
2. 服务器只执行 `git pull --ff-only`，不得手改源码。
3. 数据库迁移先升级并核验当前 revision。
4. 先重启 API 和 Shadow / 模拟 worker，验证健康与幂等状态。
5. 实盘只对 Canary 账户启用；若出现重复意图、UNKNOWN 堆积、保护缺失或账户对账失败，立即停止扩大并回滚运行版本。
6. 旧代码只有在对应新服务通过验收后删除；UI 迁移不在本轮范围。

## 9. 进度记录

| 日期 | 提交 | 结果 |
| --- | --- | --- |
| 2026-08-30 | `d13d6a2` | 建立本收尾基线；确认统一写路径已接管，但第二、第三阶段尚未完成 |
| 2026-08-30 | 本轮收尾批次 | 提升公共策略求值、决策审计和入场快照服务；切断实盘到模拟盘的私有依赖；删除旧 Binance 下单旁路；全量 Ruff 与 Pytest 通过 |
| 2026-08-31 | 模拟盘事实投影批次 | 成交事实与兼容投影拆成两阶段；新增幂等恢复、来源 execution 标识、账户级隔离和状态变化告警；全量 Ruff 与 993 项 Pytest 通过 |
| 2026-08-31 | 模拟盘余额对账批次 | 新增余额 checkpoint/流水、漂移与权益陈旧检测、账户对账状态、paper heartbeat 指标、只读审计和确认式恢复命令；历史权益缺口不伪造回填 |
| 2026-08-31 | 跨模式语义与 legacy 截止批次 | 建立八类固定金样；修复下一根 K 线执行过期信号；新建/切换模拟策略只使用 `strategy_event_v2`；0076 迁移记录 legacy 截止来源 |
| 2026-08-31 | 模拟盘历史治理批次 | 新增 applied 历史 dry-run；迁移前事实由 baseline checkpoint 封存；仅对事实完整、余额闭合的 post-checkpoint 缺失流水提供确认式幂等重建 |
| 2026-08-31 | 实盘恢复服务批次 | 抽取订单对账、持仓同步、保护恢复和账户恢复四个应用服务；保留原行为测试并新增服务测试；增加只读 `audit-live` Canary 验收命令 |
| 2026-08-31 | `5476568` 生产部署 | 服务器快进到目标提交并升级至 `0076_disable_legacy_paper`；API 与 market/ai/paper/shadow/live/ops worker 全部 active；`/api/v2/ready` 返回 ready |

## 10. 当前生产验收快照

2026-08-31 部署后的只读检查结果：

- 模拟盘审计：1 个运行账户，`blocked=false`，无漂移和告警；
- 模拟历史审计：余额闭合、无缺失账本、无需重建；
- 模拟账户信号模式：3/3 为 `strategy_event_v2`，legacy 数量为 0；
- 实盘本地恢复审计：4 个账户，`blocked=false`，UNKNOWN、长期未决执行和未保护受管持仓均为 0；
- 服务启动后 API、paper、shadow、live 最近警告日志均为空。

这些结果证明部署和本地恢复链路健康，但不替代真实交易所 Canary。当前实盘账户均处于暂停或既有错误业务状态，本次架构部署没有擅自启用真实资金交易；第三阶段最后一项仍是经过明确授权后完成独立 Canary 观察窗口。
